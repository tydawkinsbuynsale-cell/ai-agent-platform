Goal:
Expose existing agent via API + CLI using profiles.

Allowed changes:
- Add FastAPI app layer
- Add CLI that calls API
- Add APP_PROFILE config

Explicitly forbidden:
- Changes to agent logic
- Changes to eval behavior
- New AI features
- Frontend UI
